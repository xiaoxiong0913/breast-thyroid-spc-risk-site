services:
  - type: web
    name: breast-thyroid-spc-risk-site
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    pullRequestPreviewsEnabled: false
